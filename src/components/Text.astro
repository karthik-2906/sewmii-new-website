---
type Props = {
    class?: string;
};

const { class:className = "" } = Astro.props;
const baseClasses =
    "text-lg font-medium tracking-tight mb-4 max-sm:text-base";
---

<p class={`${baseClasses} ${className}`}>
    <slot />
</p>
