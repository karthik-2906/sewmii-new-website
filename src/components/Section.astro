---
type Props = {
    class?: string;
    id?:string;
};

const { class: className, id } = Astro.props;
---

<section class={className} {id}>
    <div class="mx-auto w-full max-w-7xl px-12 py-24 max-md:px-6 max-md:py-12">
        <slot />
    </div>
</section>
